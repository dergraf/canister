# Monitor mode config — used with --monitor flag.
# Used by: t_monitor.sh

[filesystem]
allow = ["/usr/lib", "/usr/bin", "/lib", "/tmp"]
deny  = ["/etc/shadow"]

[network]
egress = "proxy-only"

[process]
max_pids = 32
env_passthrough = ["PATH"]

[syscalls]

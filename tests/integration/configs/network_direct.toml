# Network direct mode — direct outbound allowed.
# Used by: t_network.sh

[filesystem]
read = ["/usr/lib", "/usr/bin", "/lib", "/tmp"]

[unsafe]
unfiltered_egress = true

[process]
env_passthrough = ["PATH", "HOME"]

[syscalls]

# Network none mode — no network, loopback only.
# Used by: t_network.sh

[filesystem]
allow = ["/usr/lib", "/usr/bin", "/lib", "/tmp"]

[network]
egress = "none"

[process]
env_passthrough = ["PATH", "HOME"]

[syscalls]

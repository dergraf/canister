[recipe]
name = "helm"
description = "helm: config + cache + data dirs; repository credentials via env"
version = "1"

[filesystem]
read = [
    "$HOME/.config/helm",
]
write = [
    "$HOME/.cache/helm",
    "$HOME/.local/share/helm",
]

[process]
env_passthrough = [
    "HELM_HOME",
    "HELM_CACHE_HOME",
    "HELM_CONFIG_HOME",
    "HELM_DATA_HOME",
    "HELM_DRIVER",
    "HELM_NAMESPACE",
    "KUBECONFIG",
]
